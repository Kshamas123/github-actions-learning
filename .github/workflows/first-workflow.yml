name: My First Workflow

on:
  push:

jobs:
    hello:
         runs-on: ubuntu-latest
         steps: 
            - run: echo "Step 1"
            - run: echo "Step 2"
            - run: echo "Step 3"